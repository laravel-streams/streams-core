{% for field in fields %}
    {% if form.fields.translations(field).enabled().isNotEmpty() %}
        <div class="field-group {{ field }}">
            {% for field in form.fields.translations(field) %}
                {{ field.render({'form': form})|raw }}
            {% endfor %}
        </div>
    {% endif %}
{% endfor %}
