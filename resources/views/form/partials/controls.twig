<div class="{{ form.options.panel_class }} controls">
    <div class="{{ form.options.panel_body_class }}">

        {% if not form.options.read_only %}
            <div class="actions">
                {{ buttons(form.actions)|raw }}
            </div>
        {% endif %}

        <div class="buttons">
            {{ buttons(form.buttons)|raw }}
        </div>

    </div>
</div>
