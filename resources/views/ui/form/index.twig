{{ asset_add("streams.js", "streams::js/form/form.js") }}

{% block content %}

    {{ form_open() }}

    {{ form_toolbar(form)|raw }}
    {{ form_layout(form)|raw }}
    {{ form_controls(form)|raw }}

    {{ form_close() }}

{% endblock %}
