{% if form.options.sections %}

    {% for section in form.options.sections %}
        {{ form_section(form, section)|raw }}
    {% endfor %}

{% else %}
    {% include "streams::ui/form/partials/section" %}
{% endif %}
