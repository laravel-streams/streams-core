<input
        type="{{ field_type.type }}"
        name="{{ field_type.name }}"
        value="{{ field_type.value }}"
        placeholder="{{ field_type.placeholder }}"
        class="form-control"
        >
