<input
        value="{{ field_type.value }}"
        name="{{ field_type.getFieldName() }}"
        type="{{ field_type.config.type ?: 'text' }}"
        placeholder="{{ trans(field_type.config.placeholder) }}"
        class="form-control"
        >
