<?php namespace Streams\Platform\Model\{namespacePrefix};

use Streams\Platform\Entry\EntryModel;

class {className} extends EntryModel
{
    protected $table = {table};

    public static $rules = {rules};

    protected $stream = {stream};

    protected $relationFields = {relationFields};

    {relations}
}
