<?php namespace Anomaly\Streams\Platform\Model\{{ namespace }};

use Anomaly\Streams\Platform\Entry\EntryModel;

class {{ class }} extends EntryModel {{ interfaces }}
{

    {{ traits }}

    protected $table = '{{ table }}';

    protected $titleKey = '{{ title }}';

    public $rules = {{ rules }};

    public $dates = {{ dates }};

    {{ translation_foreign_key }}

    {{ translation_model }}

    protected $stream = {{ stream }};

    {{ relations }}
}
