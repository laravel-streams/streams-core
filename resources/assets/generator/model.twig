<?php namespace Anomaly\Streams\Platform\Model\{{ namespace }};

use Anomaly\Streams\Platform\Entry\EntryModel;

class {{ class }} extends EntryModel
{

    protected $table = '{{ table }}';

    protected $titleKey = '{{ title }}';

    public $rules = {{ rules|raw }};

    public $dates = {{ dates|raw }};

    {{ translation_foreign_key|raw }}

    {{ translation_model|raw }}

    protected $stream = {{ stream|raw }};

    {{ relations|raw }}
}
