<?php namespace Anomaly\Streams\Platform\Model\{namespace};

use Anomaly\Streams\Platform\Entry\EntryModel;

class {class} extends EntryModel
{

    protected $table = '{table}';

    public static $rules = {rules};

    public $dates = {dates};

    {translationForeignKey}

    {translationModel}

    protected $stream = {stream};

    protected $relationFields = {relationFields};

    {relations}
}
