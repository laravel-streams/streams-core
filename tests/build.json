{
    "handle": "testing.build",
    "description": "Used for testing building streams from JSON file.",
    "config": {
        "source": {
            "path": "vendor/streams/core/tests/data/runtime",
            "format": "json"
        }
    },
    "rules": {
        "id": "required"
    },
    "fields": {
        "name": "string"
    }
}
