{
    "id": "testing.examples",
    "description": "Used for testing.",

    "config": {
        "source": {
            "path": "vendor/streams/core/tests/data/examples",
            "format": "json"
        }
    },

    "fields": [
        {
            "handle": "name",
            "type": "string",
            "rules": [ "required", "min:3" ]
        },
        {
            "handle": "age",
            "type": "integer"
        }
    ]
}
