{
    "id": "testing.examples",
    "description": "Used for testing.",
    
    "config": {
        "source": {
            "path": "vendor/streams/core/tests/data/examples",
            "format": "json"
        }
    },

    "rules": {
        "name": "required|min:3"
    },
    "validators": {},

    "fields": {
        "name": "string",
        "age": {
            "type": "integer"
        }
    }
}
