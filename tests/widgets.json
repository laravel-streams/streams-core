{
    "handle": "testing.widgets",
    "description": "Used for testing.",
    
    "source": {
        "path": "vendor/streams/core/tests/data/widgets",
        "format": "json"
    },

    "rules": {
        "name": "required"
    },

    "fields": {
        "name": "text"
    }
}
